<!--
  化学工具箱 · 关于（336×480，闪念小抄/弦电子书同款风格）
  应用信息卡（图标+名称+版本）+ 数据说明卡。静态内容，主体 absolute 顶层挂载。
-->
<template>
  <div class="page" @swipe="onSwipe">

    <!-- 应用信息卡 -->
    <div class="app-card">
      <img static src="/common/images/icon.png" class="app-icon" />
      <div class="app-info">
        <text class="app-name">化学工具箱</text>
        <text class="app-ver">V26.9.47.CALC（2609470）</text>
      </div>
    </div>

    <!-- 数据说明 -->
    <text class="sec-label">数据与运行</text>
    <div class="info-card">
      <text class="info-line">包名　com.whyy.chemcalc</text>
      <text class="info-line">运行　纯离线，计算与数据库全部内置</text>
      <text class="info-line">引擎　化合 · 分解 · 置换 · 复分解 · 氧化还原</text>
      <text class="info-line">数据　118 元素 · 252 反应 · 人教九年级全适配</text>
      <text class="info-line">存储　仅保存设置项（小数位 · 常亮 · 振动）</text>
    </div>

    <!-- 顶栏（闪念小抄同款） -->
    <img static src="/common/images/hd.png" class="hd-bg" />
    <img static src="/common/images/back.png" class="hd-back" @click="goBack" />
    <text static class="hd-sub">版本与数据</text>
    <text static class="hd-title">关于</text>

  </div>
</template>

<script>
import router from '@system.router'

export default {
  goBack() { router.back() },
  onSwipe(e) {
    if (e && e.direction === 'right') router.back()
  },
  onBackPress() {
    router.back()
    return true
  }
}
</script>

<style>
@import '../../common/style.css';

/* ---- 应用信息卡 ---- */
.app-card {
  position: absolute;
  left: 6px;
  top: 110px;
  width: 324px;
  height: 112px;
  border-radius: 36px;
  background-color: #262626;
  padding: 0 22px;
  flex-direction: row;
  align-items: center;
}
.app-icon {
  width: 64px;
  height: 64px;
}
.app-info {
  flex-direction: column;
  margin-left: 18px;
}
.app-name {
  font-size: 30px;
  font-weight: bold;
  color: #ffffff;
  lines: 1;
}
.app-ver {
  font-size: 20px;
  color: rgba(255, 255, 255, 0.55);
  lines: 1;
  margin-top: 6px;
}

/* ---- 数据说明卡 ---- */
.sec-label {
  position: absolute;
  left: 16px;
  top: 234px;
  width: 304px;
  font-size: 20px;
  line-height: 24px;
  color: rgba(255, 255, 255, 0.45);
}
.info-card {
  position: absolute;
  left: 6px;
  top: 264px;
  width: 324px;
  height: 176px;
  border-radius: 36px;
  background-color: #262626;
  padding: 10px 22px;
  flex-direction: column;
  justify-content: center;
}
.info-line {
  font-size: 20px;
  line-height: 38px;
  color: rgba(255, 255, 255, 0.85);
  lines: 1;
  text-overflow: ellipsis;
}
</style>
